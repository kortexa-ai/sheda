// Source: https://x.com/YoheiNishitsuji/status/1906149181372481797
float i, e, R, s;
vec3 q, p, d = vec3(FC.xy / r * .3 + vec2(.2, 1.8), 2);
for(q.zy --;
i ++ < 95.;
) {
o += exp(- e * 8e3) * .01 * vec4(1.2, vec3(1.16, .8, 1));
s = 5.;
p = q += d * e * R * .1;
p = vec3(log2(R = length(p)) - t * .5, exp2(R - p.z / R), atan(p.y, p.x));
for(e = -- p.y;
s < 3e2;
s += s) e += abs(dot(cos(p * s), cos(p.yzx * s)) / s * .2);
}